C <%= current_user.id %>
<% followers_ids = followers_ids || follow_user.followers.ids %>
<%= followers_ids %>
<% if followers_ids.include?(current_user.id) %>
  <small>
    <%= link_to "フォローを外す", user_relationships_path(follow_user), method: :delete, remote: true %>
  </small>
<% else %>
  <small>
    <%= link_to "フォローする", user_relationships_path(follow_user), method: :post, remote: true %>
  </small>
<% end %>
<div>
<% if current_user != follow_user && current_user.followings.include?(follow_user) %>
  <small><%= link_to 'chatを始める', chat_path(follow_user.id) %></small>
<% end %>
</div>