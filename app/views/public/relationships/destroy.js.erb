$('#relationships').html('<% if @user.followers.ids.include?(current_user.id) %><small><%= link_to "フォローを外す", user_relationships_path(@user), method: :delete, remote: true %></small><% else %><small><%= link_to "フォローする", user_relationships_path(@user), method: :post, remote: true %></small><% end %><div><% if current_user != @user && current_user.followings.include?(@user) %><small><%= link_to "chatを始める", chat_path(@user.id) %></small><% end %></div>');
$('#followCount<%= @user.id %>').html("<%= @user.followers.count %>");